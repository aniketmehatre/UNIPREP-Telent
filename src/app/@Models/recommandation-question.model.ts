export interface RecommandationQuestion {
    id: number;
    question: string;
}

export interface CurrentSpecialization {
    id: number;
    specialization_name: string;
}

export interface BasicType {
    id: number;
    name: string;
}